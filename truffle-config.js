module.exports = {
  // Configuraciones de red aquí...

  // Opciones de compilación
  contracts_build_directory: "./artifacts",  // Especifica la carpeta para los artifacts

  // Versión de Solidity
  compilers: {
    solc: {
      version: "0.8.0"  // O la versión que estés usando
    }
  }
};
